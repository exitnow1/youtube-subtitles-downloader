# 자막 배치 다운로더 사용 설명서 (초보자용)

`python yt_dlp_subtitle_downloader.py` — 영상 파일 없이 **자막만** 묶음으로 받는 도구입니다.
필요 패키지: `pip install yt-dlp browser_cookie3`

---

## 1. 파일 구성 (지도)

```
Youtube downloader/
├── yt_dlp_subtitle_downloader.py   … 자막 다운로더 본체 (이 문서의 주인공)
├── subtitle_db.py                  … 작업 기록 DB (시도·결과를 SQLite에 저장/조회)
├── subtitle_config.json            … 내 설정 (자동 생성, 메모장으로 수정)
├── subtitle_config.example.json    … 설정 기본값 본보기
├── yt_dlp_advanced_downloader_20_min.py … (별개) 영상 파일 다운로더
└── docs/                           … 변경 이력 설명서들
```

실행하면 생기는 것들 (커밋 제외, 내 PC에만 있음):

| 경로 | 내용 |
|---|---|
| `Downloads/subtitles/` (또는 지정 폴더) | 받은 자막 파일 |
| `Downloads/subtitle_jobs.db` | 시도 기록 DB |
| `Downloads/subtitle_download.log` | 실행 로그 |
| `Downloads/.last_subtitle_dir` | 지난번 저장 폴더 기억 |
| `Downloads/SUB_FAIL_LIST_시간.txt` | 그날의 실패 목록 |

## 2. 시작하면 일어나는 일 (순서대로)

```
[1] 설정 파일 읽기 (없으면 기본값으로 자동 생성)
[2] 저장 폴더 질문 → Enter=지난번, 새로 적으면 그곳에 저장
[3] 메뉴 1~5 선택
```

## 3. 모드별 사용법

### 모드 1) 채널 전체 자막

```
모드 선택: 1
자막 언어 (Enter=ko,en): ko,en
자동생성 자막도 포함? (Enter=Y): Y
형식 (Enter=vtt): vtt
인코딩 (Enter=UTF-8 / bom / cp949): (Enter)
채널 URL: https://www.youtube.com/@mkbhd
업로드 날짜 조건 (예: >= 2024-01-01, Enter=없음): >= 2024-01-01
길이 조건 (예: 5-30분, Enter=없음): (Enter)
번호 범위 (예: 1-50, Enter=전체): 1-50
```

* `@핸들/channel/c/user` 모두 가능, 뒤에 `/videos` 자동 완성
* 날짜 연산자 6종: `==  =  !=  <  <=  >  >=` (형식 `20240101` 또는 `2024-01-01`)
* 저장: `subtitles/채널명/제목 [영상ID].vtt`
* Shorts는 `/videos`탭에 없으니 주소 뒤를 `/shorts`로 직접 입력

### 모드 2) 개별 영상 자막

```
모드 선택: 2
영상 URL: https://www.youtube.com/watch?v=xxxx
(날짜/길이 조건은 선택 사항)
```

* URL에 `&list=`가 섞여 있어도 영상 1개만 받음
* 저장: `subtitles/제목 [영상ID].vtt`

### 모드 3) 재생목록 자막

```
모드 선택: 3
재생목록 URL: https://www.youtube.com/playlist?list=PLxxx
(날짜/길이/번호 조건은 채널 모드와 동일)
```

* 저장: `subtitles/재생목록명/001_제목 [영상ID].vtt` (번호 순서 유지)
* 단일 영상 주소를 넣으면 개별 모드로 자동 전환

### 모드 4) 실패 목록 재시도/예약

```
모드 선택: 4
1. [제외권장] ... → 가이드: 재시도 무의미(없는 영상) - 제외 권장
2. [예약권장] ... → 가이드: 30분 후 예약 권장 (IP 식힘 필요)
재시도할 번호: all
재시도해도 안 되는 영상 1개를 제외할까요? (Y/n): (Enter)
429 실패가 있어 30분 후 예약을 권장합니다. 예약할까요? (y/N): y
작업 스케줄러에 등록할까요? (등록하면 이 창을 닫아도 됨) (y/N): y
→ 등록 완료되면 이 창을 닫아도 됨. 아니면 앱 안에서 대기 후 실행.
```

* 표시는 `[제외권장/예약권장/쿠키권장/재시도가능]` 4종 + 요약 줄
* 예약 방식: `30분후`, `21:30` (지난 시각이면 내일)
* 재시도는 실패 당시의 언어·형식·인코딩을 그대로 사용, 조건 없이 실행

### 모드 5) 예약 관리

작업 스케줄러에 등록된 `YTSubsRetry_*` 예약을 조회·삭제합니다.

### 무질문 실행 (스케줄러용)

```
python yt_dlp_subtitle_downloader.py --retry-failed          (실패 전부)
python yt_dlp_subtitle_downloader.py --retry-failed "1,3-5"  (일부만)
```

질문·팝업 없이 실행됩니다. 없는 영상은 자동 제외됩니다.

## 4. 영상 1개가 처리되는 과정

```
[1] DB에 "이미 받은 적 있나?" 조회 → 있고 파일도 있으면 요청 0회로 스킵 ("이미 받아둠")
[2] DB에 "시도 시작" 기록 (시작 시각·제목·URL)
[3] 유튜브에 "이 영상 정보 주세요" (영상당 1회, 차단 카운터가 도는 곳)
[4] 날짜·길이 조건 검사 (내 컴퓨터 안, 요청 없음) → 탈락이면 스킵
[5] 자막 후보 순서대로 시도 (최대 4라운드)
      1순위: 요청 설정 → 2순위: 제목언어 자동 → 3순위: 요청언어 자동/제목언어 수동 → 4순위: 영어
      (제목이 한국어면 한글 우선, 영어면 영어 우선. 기본은 자동자막)
[6] 저장 + 파일 상단 헤더 기입 (완료시각·제목·URL) + 인코딩 변환
[7] DB에 "시도 종료" 기록 (종료 시각·결과·사유·파일경로)
[8] 2~5초 랜덤 대기 → 다음 영상 (20개마다 쿠키 갱신, 429 3연속이면 전체 중단)
```

* 없는 영상(삭제/비공개)은 1회로 종료, 사유를 기록 (`영상 없음...`)
* 자막이 어떤 언어에도 없으면 `자막 없음`으로 별도 분류 (실패 아님)

## 5. 결과 6종 (종료 화면)

| 표시 | 뜻 | 재시도 목록에 나오나 |
|---|---|---|
| 자막 성공 | 저장됨 | - |
| 조건 불만족으로 스킵 | 날짜·길이 조건 탈락 | - |
| 이미 받아둠 | 전에 받아서 요청 안 함 | - |
| 자막 없음 | 모든 언어 후보에 자막 없음 | 아니오 (다시 해도 없음) |
| 자막 실패 | 오류 (429·네트워크 등) | 예 (모드 4) |
| interrupted (DB에만) | 강제종료 흔적 | 아니오 (원본 모드 재실행 시 미완료분만 처리) |

## 6. 자막 파일 모습

vtt 예시 (`WEBVTT` 다음 줄이 헤더):

```
WEBVTT
NOTE Downloaded-At: 2026-09-05 20:35:00
NOTE Video-Title: 내 영상 제목
NOTE Video-URL: https://www.youtube.com/watch?v=xxxx

00:00:00.000 --> 00:00:02.000
안녕하세요
```

* srt 등은 `#` 주석 3줄이 앞에 붙습니다. 엄격한 플레이어가 경고를 내면
  메모장으로 위 4줄(주석 3줄+빈 줄)을 지우면 됩니다. vtt NOTE는 공식 스펙이라 안전.
* 인코딩: `UTF-8`(기본) / `bom`(한글TV용) / `cp949`(구형기기용).
  cp949에 없는 문자(이모지 등)는 `?`로 바뀝니다.

## 7. 설정 파일 (`subtitle_config.json`)

스크립트 옆에 자동 생성됩니다. 메모장으로 고치고 다시 실행하면 반영됩니다.

| 키 | 뜻 | 기본값 |
|---|---|---|
| `download_dir` | 저장·DB·로그 기본 위치 | `C:\Users\rpt53\Downloads` |
| `sleep_between_videos_min/max` | 영상 사이 대기(초) | 2.0 / 5.0 |
| `sleep_on_429_base` | 429 시 기본 대기(초) | 60 |
| `cookie_refresh_every_n_videos` | 쿠키 갱신 주기 | 20 |
| `sleep_retry_min/max` | 재시도 사이 대기(초) | 8.0 / 15.0 |
| `max_consecutive_429` | 연속 429 중단 기준 | 3 |
| `stale_running_minutes` | running 흔적 정리 기준(분) | 30 |
| `fallback_langs` | 마지막 후보 언어 | ["en"] |

우선순위: 실행 중 직접 입력(저장 폴더) > 설정 파일 > 코드 기본값.
깨진 파일·이상한 값은 경고 후 기본값으로 실행됩니다(죽지 않음).

## 8. 동작 원리 요약 (왜 안 막히나)

1. 영상당 요청이 "정보 1회 + 자막 1~2회"라 미디어 다운로드의 1/100 수준.
   차단 카운터가 도는 곳(player API) 외에는 가벼운 요청뿐.
2. 그래도 예의는 지킴: 영상마다 2~5초 대기, 429면 60초+ 대기 후 쿠키 갱신,
   429 3연속이면 전체 중단(차단기).
3. 조건 탈락 영상은 자막 요청 자체를 안 보냄. 이미 받은 영상은 요청 0회.
4. 로그인 쿠키로 요청하면 익명보다 신뢰도가 높음 (브라우저 닫고 실행 권장).

## 9. 주의사항

* 예정·진행 중 라이브: 날짜·길이 정보가 없어 조건 사용 시 스킵됨
* 연령제한·비공개·멤버십: 로그인 쿠키 필요. 쿠키 추출 실패 시 기존 쿠키 유지 후 진행
* 자동생성 자막: 오타·끊김 가능. 수동 자막이 있으면 수동 우선 저장
* 언어 `all`: 전 언어 저장 (파일 수십 개 가능)
* 스케줄러 예약: 실행 시각에 PC가 켜져 있어야 함
* DB 직접 보기: `subtitle_jobs.db`를 DB 브라우저나 파이썬 sqlite3로 조회 가능

## 10. 문제 해결

| 증상 | 대처 |
|---|---|
| 429가 자주 남 | 모드 4에서 30분 후 예약, 또는 설정에서 대기 시간 늘리기 |
| 쿠키 추출 실패 | 브라우저를 완전히 닫고 재실행 (DB 잠금 때문) |
| `[영상 없음]` 실패 | 삭제·비공개 영상으로 정상 기록. 재시도 불필요 |
| `[자막 없음]` | 해당 언어 자막이 정말 없음. 다른 언어 직접 지정해 보기 |
| srt가 플레이어에서 경고 | 파일 앞 `#` 3줄+빈 줄 삭제 |
| 한글이 깨져 보임(TV 등) | 인코딩 `bom` 또는 `cp949`로 다시 받기 |
