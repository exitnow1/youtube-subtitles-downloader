# 자막 배치 다운로더 가이드 (초보자용)

파일: `yt_dlp_subtitle_downloader.py` — 영상 파일 없이 **자막만** 묶음으로 받는 도구입니다.

---

## 1. 앱 전체 구조 (지도)

이 프로젝트에는 역할이 다른 스크립트 2개가 있습니다.

```
Youtube downloader/
├── yt_dlp_advanced_downloader_20_min.py  … (A) 영상 다운로더: 20분씩 잘라 영상 파일(.mp4) 저장
├── yt_dlp_subtitle_downloader.py         … (B) 자막 다운로더: 자막 파일(.vtt/.srt)만 저장
├── subtitle_db.py                        … (C) 작업 기록 DB: 시도·완료·실패를 SQLite에 저장/조회
├── SUBTITLE_GUIDE.md                     … 이 문서
├── README.md                             … (A) 설명서
└── .gitignore
```

실행 중 생기는 파일(커밋 제외): `Downloads/subtitles/`(자막),
`Downloads/subtitle_jobs.db`(DB), `Downloads/subtitle_download.log`(로그).

두 스크립트는 **같은 설계 패턴(기초 공사)을 공유**합니다.

| 공통 부품 (A에서 검증됨 → B가 그대로 가져옴) | 하는 일 |
|---|---|
| `log()` | 화면 + 로그 파일에 시간 기록 남기기 |
| `export_cookies_from_chrome()` + `_try_load_browser_cookies()` + `rotate_old_cookie()` | 브라우저 쿠키 가져오기. 임시 파일에 먼저 저장 성공 후에만 교체(실패하면 기존 쿠키 유지). Chrome→Edge→Firefox 순 폴백 |
| `fetch_flat_list()` (= A의 `fetch_info(extract_flat=True)`) | 목록만 빠르게 가져오기 (상세정보는 안 물어봄 → 요청 절약) |
| `_extract_video_url()` | 목록의 한 줄에서 실제 영상 주소 복원 (ID만 있어도 `watch?v=` 조립) |
| `_to_entries_list()` (= A의 generator 처리) | 목록이 리스트든 생성기든 안전하게 리스트로 |
| sleep/jitter + 429/403 백오프 | 영상 사이 2~5초 랜덤 대기, 429면 60초+ 대기 후 쿠키 갱신·재시도 (최대 3회) |
| `SUCCESS_LIST / FAIL_LIST` + `finalize()` | 성공·실패 집계, 실패 목록 파일 저장, 윈도우 알림 |

### 그림으로 보는 (B)의 흐름

```
[1] main() 메뉴 선택 ── 1) 채널 / 2) 개별 영상 / 3) 재생목록 / 4) 실패 재시도·예약
          │
[2] 자막 설정 묻기 ── 언어(ko,en/all) · 자동자막 포함여부 · 형식(vtt/srt)
          │
[3] 조건 묻기 ── 날짜(==,!=,<,<=,>,>=) · 길이(분) · 번호 범위 (전부 Enter면 조건 없음)
          │
[4] 쿠키 1회 준비 (export_cookies_from_chrome)
          │
[5] 채널/재생목록이면 목록만 가져오기 (fetch_flat_list, 빠름)
          │
[6] 영상마다 반복 ─────────────────────────────────────┐
          │                                            │
   ┌─ DB에 "시도 시작" 기록 (시작 시각·제목·URL) ◄──────┘
   │
   ├─ yt-dlp에 "이 영상 정보 주세요" (영상당 1회)
   │      (이때가 차단 카운터가 도는 유일한 무거운 단계)
   │
   ├─ SubtitleFilter 검사 (내 컴퓨터 안에서, 요청 없음)
   │     날짜·길이 조건 불만족 → 자막 요청 안 보내고 SKIPPED 기록
   │
   ├─ 조건 통과 → "이 자막 파일 주세요" (가벼운 요청 1~2회)
   │
   ├─ 저장 + 파일 상단에 헤더 기입 (완료시각·제목·URL)
   │     저장: subtitles/채널명/제목 [영상ID].vtt
   │
   ├─ DB에 "시도 종료" 기록 (종료 시각·결과·사유·파일경로)
   │
   ├─ 429가 3회 연속이면 나머지 중단 — 차단기 (DB에 남아 모드 4로 재개)
   │
   └─ 2~5초 랜덤 대기 → 다음 영상 (20개마다 쿠키 갱신)
          │
[7] finalize() ── 성공 / 스킵(사유 표시) / 실패 출력 + 실패 파일 + DB 경로 + 알림
```

---

## 2. 사용 흐름 (모드별)

실행: `python yt_dlp_subtitle_downloader.py`

### 모드 1) 채널 전체 — `run_channel()`

```
모드 선택 (1/2/3): 1
자막 언어 (... Enter=ko,en): ko,en
자동생성 자막도 포함? (Y/n, Enter=Y): Y
형식 (vtt/srt, Enter=vtt): vtt
채널 URL (@핸들/channel/c/user 모두 가능): https://www.youtube.com/@mkbhd
업로드 날짜 조건을 쓸까요? (예: >= 2024-01-01, 아니면 Enter): >= 2024-01-01
길이 조건을 쓸까요? (예: 5-30분, 최소만은 10-, 아니면 Enter): (Enter)
번호 범위를 지정할까요? (예: 1-50, 아니면 Enter=전체): 1-50
```

* `@mkbhd` → 자동으로 `.../@mkbhd/videos` 탭으로 정규화 (`normalize_channel_url()`)
* 날짜 연산자 6종: `==  =  !=  <  <=  >  >=` (예: `== 2024-05-01`은 그날만, `!= 2024-05-01`은 그날 제외)
* 날짜 형식: `20240101` 또는 `2024-01-01`
* 저장 위치: `Downloads/subtitles/채널명/제목 [영상ID].vtt`
* 영상 많은 채널은 목록 가져오는 데 시간이 걸립니다 (한 번뿐인 정상 과정)

### 모드 2) 개별 영상 — `run_single()`

```
모드 선택 (1/2/3): 2
영상 URL: https://www.youtube.com/watch?v=xxxx
(날짜/길이 조건은 선택 사항)
```

* URL에 `&list=`가 섞여 있어도 **영상 1개만** 받습니다 (`noplaylist=True` — 재생목록 전체를 받는 사고 방지)
* 저장 위치: `Downloads/subtitles/제목 [영상ID].vtt`

### 모드 3) 재생목록 — `run_playlist()`

```
모드 선택 (1/2/3): 3
재생목록 URL: https://www.youtube.com/playlist?list=PLxxx
(날짜/길이/번호 조건은 채널 모드와 동일)
```

* 저장 위치: `Downloads/subtitles/재생목록명/001_제목 [영상ID].vtt` (번호 순서 유지)
* 재생목록이 아니라 단일 영상 주소를 넣으면 자동으로 개별 모드로 전환됩니다

---

## 3. 동작 원리 (쉽게)

### 자막 다운로드는 "2단계 부탁"입니다

1. **1단계 — 정보 부탁** (무거운 요청, 영상당 1회):
   프로그램이 유튜브에 "이 영상의 자막 목록을 알려주세요"라고 물어봅니다.
   유튜브가 자막 주소 목록을 돌려줍니다. **차단 카운터가 도는 곳이 여기입니다.**
2. **2단계 — 파일 부탁** (가벼운 요청, 자막당 1회):
   목록에서 한국어·영어 자막 주소를 골라 파일을 받습니다.
   몇십 KB짜리 텍스트라 부담이 거의 없습니다.

### 왜 자막 모드는 안 막히나

* 영상 다운로드는 1단계 뒤에 수백~수천 개의 영상 조각 요청이 따라붙습니다.
  자막 모드는 1단계 + 자막 1~2개로 끝이라 **요청량이 1/100 수준**입니다.
* 자막 요청 자체가 면제라서가 아니라, **요청이 적어서** 안 걸리는 것입니다.
  그래서 그래도 예의는 지킵니다: 영상마다 2~5초 쉬고, 429를 맞으면 60초 이상 쉽니다.

### 조건 필터가 요청을 아끼는 방법 (`SubtitleFilter`)

날짜·길이 조건 검사는 유튜브에 묻지 않고 **내 컴퓨터 안에서** 합니다.
순서가 이렇습니다:

```
정보 받음(1단계) → 내 컴퓨터에서 조건 검사 → 불만족이면 자막 요청(2단계) 생략 → SKIPPED 기록
```

즉 조건에 안 맞는 영상은 무거운 1단계 1회만 쓰고 끝이라, 1000개 중 50개만 조건에 맞아도 자막 요청은 50번만 나갑니다.

---

## 4. 이번에 추가·개선·수정한 부분

### (a) 새로 추가된 것 (B에만 있음)

| 추가 | 위치 | 설명 |
|---|---|---|
| `skip_download` 자막 옵션 | `make_sub_ydl()` | 영상 본체는 절대 안 받음. `writesubtitles` + `writeautomaticsub` + `subtitleslangs` + `subtitlesformat` 조합 |
| `SubtitleFilter` 검사기 | 클래스 + `compare_date()` + `parse_ymd()` | 날짜 6종 연산자(`==,!=,<,<=,>,>=`) + 길이(분) 조건. yt-dlp의 `match_filter`로 연결되어 조건 불만족 영상은 자막 요청 생략. 판정 결과(`last_kept`)로 성공/스킵 분류 |
| 채널 URL 정규화 | `normalize_channel_url()` | `@핸들/channel/c/user` → `/videos` 탭 자동 완성 (shorts 등 특정 탭은 건드리지 않음) |
| 3개 모드 실행기 | `run_channel() / run_single() / run_playlist()` | 채널·개별·재생목록. 재생목록에 단일 주소를 넣으면 개별 모드로 자동 전환 |
| `SKIPPED_LIST` | 전역 + `finalize()` | "조건 불만족으로 스킵"을 실패와 분리 집계. 스킵 사유(예: `날짜 20231231가 조건(>= 20240101) 불만족`)까지 표시 |
| `noplaylist=True` (개별 모드) | `run_single()` → `download_subs_for_video()` | `&list=` 포함 주소でも 영상 1개만 처리 |
| 모드별 저장 경로 | `build_sub_outtmpl()` | 채널/`uploader`/ / 재생목록/`001_`번호 / 개별. 전부 `[영상ID]` 포함이라 동명 영상도 충돌 없음 |
| 대화형 질문 도우미 | `ask_lang_config()` + `ask_optional_filters()` | 언어·자동자막·형식·날짜·길이·범위를 순서대로 물어봄. 전부 Enter면 조건 없이 전체 |

### (b) A에서 가져와서 값만 바꾼 것 (자막은 가벼우니 완화)

| 항목 | A (영상) | B (자막) |
|---|---|---|
| 영상 간 대기 | 5~12초 | **2~5초** |
| 쿠키 갱신 주기 | 5청크 / 3영상마다 | **20영상마다** |
| 내부 sleep | 1.5~6초 | **1.0~4초** |
| 429 백오프 | 동일(60초+) | 동일(유지 — 만약을 위해) |

### (c) 없앤 것

* 20분 분할 로직 (`CHUNK_SECONDS`, `download_sections`, `_part001` 파일명) — 자막은 자를 필요가 없으니 통째로 제거. 덩달아 `ffmpeg` 필수 조건도 사라짐 (자막 변환 없이 原형식 저장).

### (d) 이번에 새로 추가된 것 2차 (실패 DB·재시도·헤더)

| 추가 | 위치 | 설명 |
|---|---|---|
| 작업 기록 DB | `subtitle_db.py` + `Downloads/subtitle_jobs.db` | 시도마다 1행: 시작시각·종료시각·제목·URL·모드·언어·결과(success/skipped/failed)·사유·자막경로·시도회차. 재시도할 때마다 새 행이 쌓여 이력 보존. 비정상 종료 시 `running`으로 남음 |
| 모드 4 실패 재시도 | `run_retry()` | DB에서 "URL별 가장 최근 실패"만 골라 번호 선택(`all / 1,3 / 2-5`) 후 재시도. 실패 당시의 언어·형식 설정을 그대로 재사용. 재시도는 조건 필터 없이 실행 (실패는 조건 문제가 아니므로) |
| 예약 실행 | `ask_schedule()` + `wait_until()` | `Enter`=지금, `30분후`, `21:30`(지난 시각이면 내일) 지원. 10초씩 끊어 대기하며 남은 시간 표시, Ctrl+C로 취소 가능 |
| 재시도 안전 장치 | `SLEEP_RETRY_MIN/MAX` + 차단기 | 재시도 간 8~15초 대기(일반 모드보다 김). 429가 3회 연속이면 나머지 중단하고 DB에 보관 → 다음 실행 모드 4로 이어서 가능 |
| 자막 파일 헤더 | `add_header_to_subtitles()` | 저장된 자막마다 상단에 완료시각·영상제목·URL 기입. vtt는 스펙 허용 NOTE 주석(`WEBVTT` 다음 줄), srt 등은 `#` 주석. 이미 헤더 있으면 중복 기입 안 함. 실제 제목·ID는 필터가 받은 정보에서 가져와 추가 요청 없음 |

---

## 6. 모드 4 사용 예시

```
모드 선택 (1/2/3/4): 4

======= 실패 목록 (URL별 가장 최근 실패) =======
1. 내 영상 제목 | https://www.youtube.com/watch?v=xxxx
   실패시각: 2026-09-05T20:35:01 | 누적시도 1회 | HTTP Error 429: Too Many Requests
2. 다른 영상 | https://www.youtube.com/watch?v=yyyy
   실패시각: 2026-09-05T20:35:40 | 누적시도 2회 | HTTP Error 403: Forbidden
재시도할 번호 (예: all / 1,3 / 2-5, Enter=전체): all
언제 재시도? (Enter=지금 / 예: 30분후 / 21:30): 30분후
예약: 2026-09-05 21:05에 재시도 시작
예약 대기 중... 29분 50초 남음 (취소: Ctrl+C)
```

자막 파일 상단은 이렇게 생깁니다 (vtt 예시):

```
WEBVTT
NOTE Downloaded-At: 2026-09-05 20:35:00
NOTE Video-Title: 내 영상 제목
NOTE Video-URL: https://www.youtube.com/watch?v=xxxx

00:00:00.000 --> 00:00:02.000
안녕하세요
```

---

## 7. 알아두면 좋은 주의사항

* **예정된 라이브·진행 중 라이브**: 업로드 날짜·길이 정보가 없어 날짜/길이 조건을 쓰면 스킵됩니다 (사유 표시됨). 조건 없이 받으면 자막이 있으면 저장됩니다.
* **Shorts**: 채널 `/videos` 탭에는 Shorts가 안 들어갑니다. Shorts 자막이 필요하면 채널 주소 뒤를 `/shorts`로 직접 입력하세요 (정규화가 그대로 둡니다).
* **연령제한·비공개·멤버십 영상**: 로그인 쿠키가 필요합니다. 시작 시 쿠키 추출이 실패하면 기존 쿠키를 유지하고 계속 진행하니, 브라우저를 닫고 다시 실행하세요.
* **자동생성 자막**: 오타·끊김이 있을 수 있는 유튜브 자동 변환본입니다. 수동 자막이 있는 영상은 수동 자막이 우선 저장됩니다.
* **언어 `all`**: 전 언어를 다 받습니다. 자막 많은 영상은 파일이 수십 개 생길 수 있습니다.
* **형식**: `vtt`(유튜브 원본) 또는 `srt`(플레이어 호환↑). 선택한 형식이 없으면 대체 형식으로 저장됩니다 (`vtt/best`, `srt/vtt/best`).
* 로그: `Downloads/subtitle_download.log`, 실패 목록: `Downloads/SUB_FAIL_LIST_<시간>.txt`
* DB: `Downloads/subtitle_jobs.db` — 프로그램이 자동 생성·관리. 파이썬 sqlite3나 DB 브라우저로 직접 조회 가능
* srt 헤더 주의: `#` 주석 3줄이 파일 맨 앞에 붙습니다. 대부분의 플레이어는 무시하지만, 엄격한 플레이어가 경고를 내면 메모장으로 위 4줄(주석 3줄+빈 줄)을 지우면 됩니다. vtt는 NOTE가 공식 스펙이라 문제없습니다
