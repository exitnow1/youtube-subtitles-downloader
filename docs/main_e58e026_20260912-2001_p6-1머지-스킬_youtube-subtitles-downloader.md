# 설명서: p6-1 작업 + 머지, 에이전트 스킬 (P6-1)

- 브랜치: `p6-1-skill` → `main`에 머지 / 머지 커밋: `e58e026` / 일시: 2026-09-12 20:01 (KST)
- 저장소: youtube-subtitles-downloader
- 한 줄 요약: 앱 전 기능을 에이전트가 부를 수 있게 `yt-subs-downloader` 스킬 발행 + README 상단에 사용법 고지. 충돌 없이 자동 머지.

---

## 1. 앱 전체 구조 (지도)

```
에이전트 요청 ("채널 자막 받아줘")
  → yt-subs-downloader 스킬 로드 (트리거 문구로 자동 발견)
  → SKILL.md의 CLI 표대로 명령 실행 (예: --channel ... --json)
  → SUMMARY_JSON + 화면 출력 읽고 사용자에게 보고
앱 코드 변경 없음 (P5-9 CLI가 이미 전제 충족). 추가물은 문서 2건뿐.
```

## 2. 사용 흐름 (이번 커밋 기준)

* 사람: README 상단의 예시 2줄이면 시작 가능.
* 에이전트: `C:\Users\rpt53\.agents\skills\yt-subs-downloader\`에 복사되어
  opencode·Codex가 자동 인식 (다음 세션부터. 금방 복사했으면 재시작 필요).
* 저장소에도 `skills/yt-subs-downloader/`로 함께 커밋되어 버전 관리됨.
  두 곳의 내용이 같은지 테스트로 확인.

## 3. 동작 원리 (쉽게)

- **스킬이란**: 에이전트용 사용 설명서입니다. "언제 이걸 쓰고, 무슨 명령을
  치고, 무엇을 조심하는지"를 정해진 양식으로 적어둡니다.
- **핵심 규칙**: 설명문에는 "언제 쓰는지"만 적고 "어떻게 하는지"는 적지
  않습니다. 과정을 요약해 두면 에이전트가 본문을 안 읽고 요약만 따라가서
  틀리기 때문입니다.
- **정확성 보장**: 문서의 플래그 25개가 실제 `--help`에 전부 있는지
  테스트로 대조합니다. 문서와 코드가 어긋나면 테스트가 깨집니다.
- **안전 수칙 내장**: 순차 실행·차단기 존중·`--missing`은 `--yes` 있을 때만
  같은 규칙을 스킬에 넣어 에이전트가 멋대로 돌리지 않게 합니다.

## 4. 전체 그림에서 뭘 추가·수정했나

| 구분 | 위치 | 내용 |
|---|---|---|
| 추가 | `skills/yt-subs-downloader/SKILL.md` | Reference형 스킬 (목적·트리거·워크플로·CLI 25종 표·안전 수칙·한계·실수 모음) |
| 수정 | `README.md` 상단 | 스킬 사용법 고지 + 예시 2개 + 설치 경로 |
| 동기화 | `C:\Users\rpt53\.agents\skills\` | 동일 내용 복사 (에이전트 인식용, repo 대상 아님) |
| 머지 | `e58e026` | `p6-1-skill` → main, 충돌 없이 자동 머지 |

## 5. 검증

- 스킬 테스트 4종 PASS: 배치 동일성(repo==agents 복사본) / frontmatter 규격
  (이름·`Use when`·500자·키워드) / 플래그 25종 `--help` 대조 / 예시 5종 파싱
- 머지 후 회귀: CLI 전종 PASS (앱 코드 미변경)
- `py_compile` 해당 없음(문서만), Gitleaks 통과, `f2b6fd2..e58e026 main -> main` 푸시 확인
- 주의: 스킬 목록은 세션 시작 시 스캔되므로, 복사 직후 현 세션에서는
  미인식 → 다음 세션부터 `yt-subs-downloader` 호출 가능
