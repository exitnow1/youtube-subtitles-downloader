# 설명서: p2-3 머지, 예약 영속 main 통합

- 브랜치: `main` / 머지 커밋: `ee173b5` / 일시: 2026-09-05 22:06 (KST)
- 저장소: youtube-subtitles-downloader
- 한 줄 요약: `p2-3-persistent-schedule` 브랜치(무질문 CLI + 스케줄러 등록/관리 + 모드 5)를 main에 합침. 충돌 없이 자동 머지.

---

## 1. 앱 전체 구조 (지도)

```
main (a14f9b6, p2-1·p2-2 합친 상태) ─┐
                                      ├→ 머지 ee173b5 → main이 전부 가짐
p2-3 (3667f9d 기반, 스케줄러 2커밋) ───┘
```

머지 후 모드 4 끝부분 흐름:

```
예약 시각 확정 → "작업 스케줄러에 등록할까요? (y)"
  → 예 + 등록 성공: 스케줄러가 맡음, 이 창 닫아도 됨 (앱 대기는 생략)
  → 아니오/실패: 기존대로 앱 안에서 대기
모드 5 (신규): 등록된 YTSubsRetry_* 조회·삭제
```

## 2. 사용 흐름 (이번 머지 기준)

p2-3 브랜치에서 보던 화면이 main에서도 그대로 나옵니다.
`schtasks` 명령 예: `"python" "....py" --retry-failed "1,3" --headless`.

## 3. 동작 원리 (쉽게)

- **왜 충돌이 없었나**: p2-3이 건드린 곳(스케줄러 함수·CLI·메뉴 5·실행 본체 분리)과,
  그 사이 main에 들어온 변경(p2-1 폴백 반복문·p2-2 안내 UI)은 겹치는 줄이
  없었습니다. 단, 합친 뒤에는 질문이 1개 늘어서(스케줄러 등록 제안)
  기존 P2-2 테스트의 입력이 모자라 StopIteration이 났고, 테스트 입력 1개를
  추가해 해결했습니다 (코드 버그 아님).
- **머지란**: p2-3의 2개 커밋을 main 끝에 이어 붙이는 것 (`--no-ff` 기록 유지).

## 4. 전체 그림에서 뭘 합쳤나

| 가져온 커밋 | 내용 |
|---|---|
| `135ebc4` | 무질문 CLI + 스케줄러 등록/조회/삭제 + 모드 5 + 공용 실행기 분리 |
| `7c37bbc` | 위 설명 문서 |

## 5. 검증

- 머지 후 모의 테스트 8종 PASS: P2-3·P2-2(입력 1개 추가 후)·제목언어·
  없는영상·P1-1·P1-2·DB/헤더/재시도·저장위치
- `py_compile` 정상, `a14f9b6..ee173b5 main -> main` 푸시 확인
