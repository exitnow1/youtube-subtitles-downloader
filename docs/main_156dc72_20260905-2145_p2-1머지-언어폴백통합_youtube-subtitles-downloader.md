# 설명서: p2-1 머지, 언어 폴백 main 통합

- 브랜치: `main` / 머지 커밋: `156dc72` / 일시: 2026-09-05 21:45 (KST)
- 저장소: youtube-subtitles-downloader
- 한 줄 요약: `p2-1-lang-fallback` 브랜치(언어 폴백 + no_subtitle + 제목언어)를 main에 합침. 충돌 없이 자동 머지.

---

## 1. 앱 전체 구조 (지도)

```
main (ba59fb0) ── 저장위치 지정(d2e33b1) ──┐
                                            ├→ 머지 156dc72 → main이 둘 다 가짐
p2-1 (ba59fb0) ── 폴백 4커밋(93736b2~a8cd8d7) ┘
```

머지 후 main의 자막 처리 순서:

```
DB 캐시 확인 → DB 시작 기록 → 1순위(요청 설정) → 제목 보고 나머지 후보 동적 생성
(제목언어 자동 → 요청언어 자동 → 제목언어 수동 → 영어 자동)
→ 파일 있으면 저장+헤더, 전부 비면 no_subtitle
```

## 2. 사용 흐름 (이번 머지 기준)

사용자 입력 변화 없음. 달라지는 건 결과입니다.

```
이전 main: 요청 언어에 자막 0개여도 "성공" 기록
이후 main: 요청 → 자동 → 영어 순으로 더 물어보고, 그래도 없으면
          "자막 없음"으로 별도 분류 (재시도 목록 제외)
```

## 3. 동작 원리 (쉽게)

- **왜 충돌이 없었나**: main 쪽 변경(저장 폴더 질문·전역 확정)과 p2-1 쪽
  변경(다운로드 반복문·폴백)은 건드린 줄이 완전히 달랐습니다.
  Git은 겹치지 않는 변경을 자동으로 합칩니다.
- **머지란**: 갈라져 있던 p2-1의 4개 커밋을 main 끝에 이어 붙이는 것
  (`--no-ff`로 합친 기록을 남김). 기존 커밋의 해시는 그대로라 히스토리가
  보존됩니다.

## 4. 전체 그림에서 뭘 합쳤나

| 가져온 커밋 | 내용 |
|---|---|
| `93736b2` | 폴백 후보 순서 + `no_subtitle` 분류 + `_download_round()` 분리 |
| `3c81a1d` | 위 설명 문서 |
| `035c711` | 제목 언어 판별 + 자동 우선 동적 후보 (`detect_title_lang`, `_build_title_rest`) |
| `a8cd8d7` | 위 설명 문서 |
| + 각 브랜치 문서 2건 | `docs/`로 함께 들어옴 |

main 고유 기능(저장위치 지정·DB·재시도·헤더·없는영상·캐시)은 그대로 유지.
합친 뒤 양쪽 기능 존재를 코드에서 직접 확인
(`detect_title_lang`·`NO_SUB_LIST`·`ask_save_location`·`UNAVAILABLE_PATTERNS` 등).

## 5. 검증

- 머지 후 모의 테스트 6종 PASS: 제목언어·없는영상·P1-1캐시·P1-2·DB/헤더/재시도·저장위치
- `py_compile` 정상, `3667f9d..156dc72 main -> main` 푸시 확인
